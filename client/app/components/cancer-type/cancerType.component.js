import template from './cancerType.html';
import controller from './cancerType.controller';

let cancerTypeComponent = {
  restrict: 'E',
  bindings: {},
  template,
  controller
};

export default cancerTypeComponent;
